<ul id="books">
	<div class="shelf">
	<% @book.each_with_index do |book, i| %>
		<% if i % 6 == 0 && i != 0 %>
			</div>
			<div class="shelf">
		<% end %>
		<li><%= link_to image_tag("/images/twilight_book_cover.jpg", :border => 0, :title => book.title), book %></li>
	<% end %>
	</div>
</ul>
